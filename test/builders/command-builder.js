const { assert, expect } = require('chai');

describe('Command Builder', function() {

    describe('constructor(name)', function() {
        it('passes when the command accepts the name', () => {

        });
    });

});
